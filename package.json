{
    "name": "css.pagepan.com",
    "version": "1.0.0",
    "source": [
        "src/pagepan.scss",
        "src/pagepan-polyfill.scss",
        "src/pageyun.scss"
    ],
    "browserslist": "> 0.5%, last 20 versions, not dead",
    "scripts": {
        "start": "parcel",
        "watch": "parcel watch",
        "build": "parcel build --no-source-maps",
        "build-ui": "parcel build ./src/pagepan-all.scss --no-source-maps --dist-dir ../ui.pagepan.com/src/assets/css"
    },
    "devDependencies": {
        "parcel": "^2.12.0"
    }
}
